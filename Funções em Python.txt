
Python Aula Funcao

Funçoes em Python
Para defir uma funçao em Python utilizamos a seguinte maneira:
def mensagem():
    print('Bom dia')


mensagem()
def recebetexto(txt):
    print(txt)


recebetexto('Bom dia amigos')

# funcao com retorno
def divisao(v1, v2):
    if v2 > 0:
        return v1 / v2

# chamando a função passando valores
# podemos receber o valor de retorno em uma variavel local


op_divisao = divisao(8, 3)
# Sendo assim podemos utilizar este valor retornado para outras funcionalidades
if op_divisao:
    print(op_divisao)
else:
    print('Ops deu pala')

Atividade:
Faça um programa que receba como dados de entrada dois numeros e mostre como resultado :
a)a soma dos numeros
b)a Subtração dos numeros
c)A multiplicaçao dos numeros
d)A media dos numeros
Observação utilizar Funcao

Solução

def calculos(op, v1, v2):
    if op == 1:
        return v1 + v2
    elif op == 2:
        return v1 - v2
    elif op == 3:
        return v1 * v2
    elif op == 4:
        return (v1 + v2)/2
    else:
        return 0

# chamando a função passando valores
# podemos receber o valor de retorno em uma variavel local

print(calculos(4, 8, 3))


===============Com input =============
def soma(num1, num2):
    return print(f"Soma: {num1} + {num2} = ", num1+num2)
def subtracao(num1, num2):
    return print (f"Subtração: {num1} - {num2} = ", num1-num2)
def multiplicacao (num1, num2):
    return print (f"Multiplicação: {num1} * {num2} = ", num1*num2)
def divisao(num1, num2):
    return print (f"Divisão: {num1} / {num2} = ", num1/num2)
print('--------------------------------')
print('--------------------------------')
print('      --  Calculadora   --    ')
print('--------------------------------')
num1 = int(input('Digite o primeito numero: '))
num2 = int(input('Digite o segundo numero: '))
print (' ( + ) adição')
print (' ( - ) subtração')
print (' ( * ) multiplicação')
print (' ( / ) divisão')
operador = input ('Escolha uma operação :')
if operador == '+':
    soma(num1, num2)
elif operador == '-':
     subtracao(num1, num2)
elif operador == '*':
     multiplicacao(num1, num2)
elif operador == '/':
     divisao(num1, num2)
else:
    print('Operador desconhecido')

==============
Melhorando o Codigo

def calc(op, num1, num2):
    if(op == '+'):
        return print(f"Soma: {num1} + {num2} = ", num1+num2)

    if(op == '-'):
        return print(f"Soma: {num1} - {num2} = ", num1-num2)

    if(op == '*'):
        return print(f"Soma: {num1} * {num2} = ", num1*num2)

    if(op == '/'):
        if (num2 > 0):
            return print(f"Divisão: {num1} / {num2} = ", num1/num2)
        else:
            return print(f"Erro na Divisão: ", 0)


sair = '1'
while (sair != '0'):
    print('--------------------------------')
    print('--------------------------------')
    print('      --  Calculadora   --    ')
    print('--------------------------------')
    num1 = int(input('Digite o primeito numero: '))
    num2 = int(input('Digite o segundo numero: '))
    print(' ( + ) adição')
    print(' ( - ) subtração')
    print(' ( * ) multiplicação')
    print(' ( / ) divisão')
    operador = input('Escolha uma operação :')
    calc(operador, num1, num2)

    sair = input(
        'Digite 0 para sair  ou 1 Para continuar :')
    print('--------------------------------------------------------')

===============
# importando modulos

from random import randint
for i in range(6):
    soreteado = randint(1, 60)
    print(soreteado)
=========================

# Funçoes anonimas ou lambda é utilizada para passar uma funçao como parametro para outra função

def funcao(v1, v2):  # exemplo de funçao padrao
    return v1 * v2

resultado = funcao(2, 3)
print(resultado)

# Funçoes anonimas ou lambda exemplo

def resultado_1(n1, n2): return n1*n2


print(resultado_1(2, 3))
# perceba que o resultado é o mesmo mas a função não tem nome apenas
# chamamos o nome da variavel ou propriedade e ela é executada



# outro exemplo
produtos = [
    ['p1', 128],
    ['p2', 430],
    ['p3', 318],
    ['p4', 785],
    ['p5', 855],
]
# utilizano lanbda para passar os prdutos como parametro e imprimindo-os pelo indice nome
print(sorted(produtos, key=lambda index: index[0]))



# utilizando lanbda para passar os prdutos como parametro e imprimindo-os pelo preço
print(sorted(produtos, key=lambda index: index[1], reverse=True))
#observação o index[1] já funciona como o comando return

=============================

# recurso legal para desempacotamento lista comprehension python
carrinho = []
carrinho.append(('p1', 90))
carrinho.append(('p2', 34))
carrinho.append(('p3', 580))
carrinho.append(('p4', 102))

soma = sum([float(valores) for item, valores in carrinho])
print(carrinho)
print('Valor total dos produtos', soma)




# Outro exemplo list comprehension python somando listas

pedido1 = [125, 132, 98, 10]
pedido2 = [25, 28, 102, 35, 50, 65]

# maneira peba
lista_soma = []
for i in range(len(pedido1)):
    lista_soma.append(pedido1[i]+pedido2[i])
print(lista_soma)

# maneira pro
lista_soma_pro = [x+y for x, y in zip(pedido2, pedido1)]
print(lista_soma_pro)

=============================
salve este arquivo com o nome de dados.py
# Map python linkando arquivos
produtos = [
    {'nome': 'p1', 'preco': 10},
    {'nome': 'p2', 'preco': 10},
    {'nome': 'p3', 'preco': 10},
    {'nome': 'p4', 'preco': 10},
    {'nome': 'p5', 'preco': 10},
]

clientes = [
    {'nome': 'Ana', 'idade': 18},
    {'nome': 'Maria', 'idade': 20},
    {'nome': 'Jose', 'idade': 35},
    {'nome': 'Pebinha', 'idade': 16},
    {'nome': 'juquinha', 'idade': 12},
]

Agora vamos criar um arquivo para utilizar os dados
principal.py

from dados import produtos, clientes

for produto in produtos:
    print(produto)
# com isto estamos buscando os produtos no arquivo dados e imprimindo no arquivo principal
#Buscando os preços com map

precos = map(lambda p: p['preco'], produtos)

for preco in precos:
    print(preco)

================================
# utilizando  filter listando produtos com precos maiores que 100

nova_lista = filter(lambda p: p['preco'] > 100, produtos)

for produto in nova_lista:
    print(produto)

# utilizando  filter com um função
def filtrando(produto):
    if produto['preco'] > 50:
        return True


# chamando a funçao
nova_lista = filter(filtrando, produtos)

for produto in nova_lista:
    print(produto)
